defmodule Portal.Repo.Migrations.AddSystemResultDuration do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    alter table(:catalog_system_results) do
      add(:duration_sec, :float)
    end
  end

  def down do
    alter table(:catalog_system_results) do
      remove(:duration_sec)
    end
  end
end
