defmodule Portal.Repo.Migrations.AddSystemResultPhaseTimings do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    alter table(:catalog_system_results) do
      add(:phase_timings, :map)
    end
  end

  def down do
    alter table(:catalog_system_results) do
      remove(:phase_timings)
    end
  end
end
